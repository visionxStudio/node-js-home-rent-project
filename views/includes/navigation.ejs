<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-capitalize">
    <a class="navbar-brand" href="/">Home-Rent</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse ml-4" id="navbarNavAltMarkup">
        <div class="navbar-nav text-center">
            <% if (isAuthenticated) { %>
                <a class="nav-item nav-link ml-2  <%= path === '/' ? 'active text-white ' : '' %>" href="/">Home</a>
            <a class="nav-item nav-link ml-2 <%= path === 'admin/add-client' ? 'active text-white ' : '' %>" href="/admin/add-client">Add Client</a>
            <% } %>
        </div>
        <div class="navbar-nav text-center ml-auto">
            <% if (!isAuthenticated) { %>
            <a class="nav-item nav-link mr-2 <%= path === '/login' ? 'active text-white ' : '' %>"
                href="/login">Login</a>
            <a class="nav-item nav-link mr-2 <%= path === '/signup' ? 'active text-white ' : '' %>"
                href="/signup">Signup</a>
            <% } else { %>
            <form action="/logout" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <a class="nav-item nav-link mr-2" href="/logout">Logout</a>
            </form>
            <% } %>
        </div>
    </div>
</nav>